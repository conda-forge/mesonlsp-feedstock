context:
  version: "5.0.0"

package:
  name: mesonlsp
  version: ${{ version }}

source:
  url: https://github.com/JCWasmx86/mesonlsp/archive/v${{ version }}.tar.gz
  sha256: d45b85c525393cc24c446b813a6c09fe8cd252153207b77b486f77173d9ab8b4

build:
  number: 0
  skip: win

requirements:
  build:
    - ${{ compiler('c') }}
    - ${{ compiler('cxx') }}
    - ${{ stdlib('c') }}
    - git
    - meson
    - ninja
    - pkg-config
    - if: unix
      then: sed
  host:
    - libarchive
    - libcurl
    - if: osx
      then: fmt
    - lsprotocol
    - nlohmann_json
    - pkgconf
    - pygls
    - libuuid
    - zlib

tests:
  - script:
      - mesonlsp --help

about:
  license: GPL-3.0-or-later
  license_file: COPYING
  summary: An unofficial, unendorsed language server for meson written in C++
  homepage: https://github.com/JCWasmx86/mesonlsp
  repository: https://github.com/JCWasmx86/mesonlsp

extra:
  recipe-maintainers:
    - danielnachun
