context:
  version: "5.0.1"

package:
  name: mesonlsp
  version: ${{ version }}

source:
  url: https://github.com/JCWasmx86/mesonlsp/archive/v${{ version }}.tar.gz
  sha256: d4868b866562fe2ab2ec59f928c92f2f018f870a3a9a6a1e2ca5dee106601a57

build:
  number: 0
  skip: win

requirements:
  build:
    - ${{ compiler('c') }}
    - ${{ compiler('cxx') }}
    - ${{ stdlib('c') }}
    - git
    - meson
    - ninja
    - pkg-config
    - if: unix
      then: sed
  host:
    - libarchive
    - libcurl
    - if: osx
      then: fmt
    - lsprotocol
    - nlohmann_json
    - pkgconf
    - pygls
    - libuuid
    - zlib

tests:
  - script:
      - mesonlsp --help

about:
  license: GPL-3.0-or-later
  license_file: COPYING
  summary: An unofficial, unendorsed language server for meson written in C++
  homepage: https://github.com/JCWasmx86/mesonlsp
  repository: https://github.com/JCWasmx86/mesonlsp

extra:
  recipe-maintainers:
    - danielnachun
