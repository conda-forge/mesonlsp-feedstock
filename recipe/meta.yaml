{% set name = "mesonlsp" %}
{% set version = "4.3.2" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://github.com/JCWasmx86/{{ name }}/archive/v{{ version }}.tar.gz
  sha256: 1472432d4604fe556bbf68710db0730adb9f0e81d170efde5d97e212014734e2

build:
  number: 0
  skip: true  # [win]

requirements:
  build:
    - {{ compiler('c') }}
    - {{ compiler('cxx') }}
    - {{ stdlib('c') }}
    - meson
    - ninja
    - pkg-config
    - sed  # [unix]
  host:
    - libarchive
    - libcurl
    - fmt  # [osx]
    - lsprotocol
    - nlohmann_json
    - pkgconf
    - pygls
    - libuuid

test:
  commands:
    - {{ name }} --help

about:
  home: https://github.com/JCWasmx86/{{ name }}
  license: GPL-3.0-or-later
  license_file: COPYING
  summary: An unofficial, unendorsed language server for meson written in C++
  dev_url: https://github.com/JCWasmx86/{{ name }}

extra:
  recipe-maintainers:
    - danielnachun
